# This is a basic workflow that is manually triggered
name: VPS SSHX 24/7
on:
push:

branches: [ "main" ]
pull request:
branches:
[ "main" ]
workflow_dispatch:
Jobs:
root_apolinario:
runs-on: ubuntu-latest
steps:
uses: actions/checkout@v4
name: Iniciando a VPS 24/7
run: curl -sSf https://sshx.io/get sh-s run
